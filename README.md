# cutie
Browser engine

# How to build with CMake

In the terminal at the project directory:

1. Run `cmake ../cutie` to configure the project and generate a native build system
1. Run `cmake --build .` to compile and link the project
1. Finally, run the name of the executable file